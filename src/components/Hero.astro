<section class="hero">
    <div class="hero-backgrounds">
        <div class="bg-image bg-1"></div>
        <div class="bg-image bg-2"></div>
        <div class="bg-image bg-3"></div>
        <div class="bg-image bg-4"></div>
    </div>
    <div class="hero-overlay"></div>
    <div class="container hero-content animate-on-scroll">
        <h1>Vive bajo una <br/><span class="text-accent">cúpula única</span></h1>
        <p>
            Domos geodésicos llave en mano en todo Chile. Diseño, eficiencia y
            confort para tu hogar o proyecto turístico de alto nivel.
        </p>
        <div class="hero-buttons">
            <a href="#domos" class="btn btn-secondary">Explorar Modelos</a>
            <a href="#contacto" class="btn btn-outline" style="color: white; border-color: white;">Contáctanos</a>
        </div>
    </div>
</section>

<style>
    .hero {
        position: relative;
        min-height: 100vh; /* Changed from 80vh to fullscreen */
        display: flex;
        align-items: center;
        color: var(--white);
        padding: 6rem 0;
        overflow: hidden;
    }

    .hero-backgrounds {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
    }

    .bg-image {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        opacity: 0;
        animation: kenburns-crossfade 24s infinite;
    }

    /* Set the specific images */
    .bg-1 {
        background-image: url('/hero-domo-1.jpeg');
        animation-delay: 0s;
    }
    .bg-2 {
        background-image: url('/hero-domo-2.jpeg');
        animation-delay: 6s;
    }
    .bg-3 {
        background-image: url('/hero-domo-3.jpeg');
        animation-delay: 12s;
    }
    .bg-4 {
        background-image: url('/hero-domo-4.jpeg');
        animation-delay: 18s;
    }

    @keyframes kenburns-crossfade {
        0% { opacity: 0; transform: scale(1.0); }
        8% { opacity: 1; }
        25% { opacity: 1; transform: scale(1.05); }
        33% { opacity: 0; transform: scale(1.08); }
        100% { opacity: 0; transform: scale(1.08); }
    }

    .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(
            135deg, 
            rgba(18, 43, 39, 0.9) 0%, 
            rgba(30, 69, 62, 0.5) 100%
        );
        z-index: 1;
    }

    .hero-content {
        position: relative;
        z-index: 2;
        text-align: center;
        max-width: 900px;
        margin: 0 auto;
        padding-top: 5rem; /* Space for the integrated header */
    }

    .hero h1 {
        font-size: clamp(3rem, 6vw, 5.5rem);
        color: var(--white);
        margin-bottom: 1.5rem;
        line-height: 1.1;
        text-shadow: 0 4px 20px rgba(0,0,0,0.3);
    }

    .text-accent {
        color: var(--secondary);
    }

    .hero p {
        font-size: clamp(1.1rem, 2vw, 1.25rem);
        max-width: 650px;
        margin: 0 auto 3rem;
        color: rgba(255, 255, 255, 0.9);
        text-shadow: 0 2px 10px rgba(0,0,0,0.2);
        font-weight: 300;
    }

    .hero-buttons {
        display: flex;
        gap: 1.5rem;
        justify-content: center;
        flex-wrap: wrap;
    }

    @media (max-width: 768px) {
        .bg-image {
            background-attachment: scroll; /* Disabled on mobile for performance */
        }
        .hero {
            /* Full screen on mobile, subtracting approx header height */
            min-height: calc(100svh - 75px);
            padding: 2rem 0; /* reduce padding so content fits */
        }
        .hero-content {
            padding-top: 0; /* Header is not integrated on mobile, no need for top padding */
        }
    }
</style>
